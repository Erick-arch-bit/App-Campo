// Entry point manejado por expo-router/entry (configurado en package.json "main")
// Este archivo ya no es necesario como entry point manual.
// Expo Router se encarga de registrar el componente raíz automáticamente.
import 'expo-router/entry'
